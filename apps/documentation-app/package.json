{
  "name": "@kaapi-monorepo-playground/documentation-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsup src --format esm --outDir dist --clean --metafile --dts --sourcemap",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@kaapi/kaapi": "^0.0.40",
    "joi": "^18.0.2"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10"
  }
}