{
  "name": "@kaapi-monorepo-playground/messaging-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsup src --format esm --outDir dist --clean --metafile --dts --sourcemap",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@hapi/boom": "^10.0.1",
    "@kaapi/kaapi": "^0.0.41",
    "@kaapi/kafka-messaging": "^0.0.41",
    "@kaapi/validator-zod": "^0.0.41",
    "kafkajs": "^2.2.4",
    "winston": "^3.19.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10"
  }
}