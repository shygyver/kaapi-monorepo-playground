{
  "name": "@kaapi-monorepo-playground/notification-app",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && tsup src --format esm --outDir dist --clean --metafile --dts --sourcemap",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint ."
  },
  "dependencies": {
    "@kaapi/kafka-messaging": "^0.0.41",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10"
  }
}